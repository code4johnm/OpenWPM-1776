# C-SCRM intake

New conda, pip, npm, Playwright, base-image, syft, grype, or cosign additions require a filled `TEMPLATE.md` in the same PR as the pin change.

Allowlisted indexes: conda-forge and PyPI (plus npm for markdown tooling only).
